@using Bilboard.ViewModels
@inject NavigationManager Navigation
@inject HttpClient Client

<EditForm Model="@SignUpVM" OnValidSubmit="HandleSignUp" class="needs-validation" novalidate>
    <DataAnnotationsValidator />

    <div class="form-floating mb-3">
        <InputText @bind-Value="SignUpVM.FirstName" @oninput="(e => OnCheckEmailInput(((ChangeEventArgs)e).Value?.ToString()))" id="Firstname" type="email" class="@(EmailValid + " form-control")" placeholder="Firstname" required />
        <label for="Firstname">Firstname</label>
        <div class="invalid-tooltip">
            Must be a valid first name.
        </div>
    </div>

    <div class="form-floating mb-3">
        <InputText @bind-Value="SignUpVM.LastName" @oninput="(e => OnCheckEmailInput(((ChangeEventArgs)e).Value?.ToString()))" id="Lastname" type="email" class="@(EmailValid + " form-control")" placeholder="Lastname" required />
        <label for="Lastname">Lastname</label>
        <div class="invalid-tooltip">
            Must be a valid last name.
        </div>
    </div>

    <div class="form-floating mb-3">
        <InputText @bind-Value="SignUpVM.Email" @oninput="(e => OnCheckEmailInput(((ChangeEventArgs)e).Value?.ToString()))" id="Username" type="email" class="@(EmailValid + " form-control")" placeholder="Username" required />
        <label for="Username">Email</label>
        <div class="invalid-tooltip">
            Must be a valid email address.
        </div>
    </div>

    <div class="form-floating mb-3">
        <InputText @bind-value="SignUpVM.Password" @oninput="(e => OnCheckPasswordInput(((ChangeEventArgs)e).Value?.ToString()))" id="Password" type="password" class="@(PassValid + " form-control")" placeholder="Password" required />
        <label for="Password">Password</label>
        <div class="invalid-tooltip">
            Must includes capitals and numbers.
        </div>
    </div>

    <div class="form-floating mb-3">
        <InputText @bind-value="SignUpVM.Password" @oninput="(e => OnCheckPasswordInput(((ChangeEventArgs)e).Value?.ToString()))" id="ConfirmPassword" type="password" class="@(PassValid + " form-control")" placeholder="Confirm Password" required />
        <label for="ConfirmPassword">Confirm Password</label>
        <div class="invalid-tooltip">
            Must includes capitals and numbers.
        </div>
    </div>
    <div class="form-group mb-3 text-center">
        <button type="submit" class="@("btn border btn-block")">
            <span class="@(Loading + " spinner-border spinner-border-sm")" aria-hidden="true"></span>
            Sign Up
        </button>
    </div>
    <div class="text-center">
        <a class="d-block" href="signin">Sign In</a>
        <a class="d-block" href="forgot">forgot your password?</a>
    </div>
</EditForm>

@code {
    private SignUpViewModel SignUpVM = new();
    public string EmailValid { get; set; } = string.Empty;
    public string PassValid { get; set; } = string.Empty;
    public string Loading { get; set; } = "d-none";

    // Callback delegates from parent
    [Parameter]
    public Func<string, Task>? OnShowToast { get; set; }

    [Parameter]
    public Func<string, Task<bool>>? OnCheckEmailInput { get; set; }

    [Parameter]
    public Func<string, Task<bool>>? OnCheckPasswordInput { get; set; }

    [Parameter]
    public Func<Task>? OnHandleSignIn { get; set; }

    private async Task HandleSignUp()
    {
        var EmailValidation = await OnCheckEmailInput(SignUpVM.Email);
        var PassValidation = await OnCheckPasswordInput(SignUpVM.Password);
        if (EmailValidation && PassValidation)
        {
            Loading = string.Empty;
            var response = await Client.PostAsJsonAsync("/api/Account/SignUp", SignUpVM);
            if (response.IsSuccessStatusCode)
            {
                Console.WriteLine("Sign up successful");
                EmailValid = "valid";
                PassValid = "valid";
                await Task.Delay(1000);
                if (OnShowToast != null)
                await OnShowToast("Account created successfully!");
                Loading = "d-none";
                StateHasChanged();
                await Task.Delay(2000);
                Navigation.NavigateTo("/signin");
            }
            else
            {
                Console.WriteLine("Sign in failed");
                EmailValid = "is-invalid";
                PassValid = "is-invalid";
                Loading = "d-none";
                StateHasChanged();
                // Handle error
                if (OnShowToast != null)
                    await OnShowToast("Incorrect details.");
            }
        }
        else
        {
            // Validation failed
            Console.WriteLine("Validation failed");
            if (OnShowToast != null)
                await OnShowToast("The username or password is not valid.");
            // Handle validation error
        }
    }
}
